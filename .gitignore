# Python bytecode
__pycache__/
*.py[cod]

# Generated SQLite build of the seed data (rebuild with: make db)
data/processed/*.db

# SQLite sidecar files (WAL mode)
data/processed/*.db-shm
data/processed/*.db-wal

# Dependencies are installed, not committed. 250 files / 24.9 MB of
# node_modules were committed on the branch this merge came from.
node_modules/
